# Capability Matrix — Reality Engine (world-compiler spec)

Tracks only the **Exit Goals (A-L)** from the autonomous-implementation-loop
directive — that's what decides whether to keep looping, not the full
40-section spec. Statuses: NOT_STARTED / SKELETON / PARTIAL / FUNCTIONAL /
INTEGRATED / VALIDATED.

| Goal | What it needs | Status | Evidence |
|---|---|---|---|
| A — Evidence system | Dataset, Session, evidence persists/reopens | **FUNCTIONAL** | `evidence/session.py`, `evidence/dataset.py`. Create/reopen round-trip tested (`test_serialize_deserialize_roundtrip_preserves_evidence_and_history`, `test_dataset_roundtrip_preserves_sessions_and_evidence`). Not yet wired to a real file importer (no photo/video ingestion path) — evidence items reference a `source_uri`, nothing reads the actual bytes yet. |
| B — Session merging | Select, merge, preserve provenance, detect conflicts | **PARTIAL** | `Dataset.merge()` produces a real `MergedContext`; source sessions verified untouched (`test_merge_preserves_source_session_provenance`); coordinate-frame conflict detection is real (`test_merge_flags_coordinate_frame_conflict`). Geometric registration/feature-matching/scale reconciliation is explicitly **not implemented** — `registration_status: "not_performed"`, honestly reported rather than faked. |
| C — Real reconstruction | End-to-end evidence → geometry | PARTIAL | `reconstruction/backend/fake.py` exercises the pipeline shape (6 tests). `reconstruction/backend/colmap_backend.py` is a real implementation — shells out to `feature_extractor`/`exhaustive_matcher`/`mapper`/`model_converter`, parses COLMAP's documented text output (`images.txt`, `points3D.txt`) into typed poses/points. **COLMAP 4.2.0 (no-CUDA Windows build) is now installed and the real subprocess pipeline has been run end-to-end** against 4 real photos of a public test scene: 3/4 images registered, 228 real 3D points produced, ~23s wall time. Two real bugs found and fixed by that run: (1) COLMAP's Windows CLI constructs a QApplication and blocks on a GUI dialog unless `QT_QPA_PLATFORM=offscreen`/`QT_PLUGIN_PATH` are set; (2) this build has no GPU support, so `--FeatureExtraction.use_gpu 0`/`--FeatureMatching.use_gpu 0` must be passed or every step fails. Both fixed unconditionally in `colmap_backend.py` (`_subprocess_env()`, `use_gpu` constructor flag, default `False`). Still PARTIAL, honestly: only one 4-image scene has been exercised (not a stress test, not a large building), and no automated test in `tests/` invokes the real binary — committed tests still only cover the pure parsing functions against fixture text. |
| D — WorldIR from reconstruction | Reconstructed result becomes typed entities+relationships+provenance+confidence | PARTIAL | Two real write paths now: `evidence/promote.py` (`MANUAL_MEASUREMENT` → `Entity`+`Material`+`Measurement`) and `evidence/promote_reconstruction.py` (`ReconstructionResult` → `Entity`+point-cloud `Geometry`, provenance forced to `RECONSTRUCTED`, refuses to promote a failed/empty result via `EmptyReconstructionError`). Both round-trip through `WorldIR.to_dict()`. Photo/video evidence still can't be promoted directly — it must go through a real `IReconstructionBackend` first, and only the fake one exists. |
| E — Incremental world growth | New Session added without destroying prior evidence | **FUNCTIONAL** | `Dataset.add_session()` only appends; `Session.add_evidence()` only appends; nothing in this layer supports deletion. Not yet exercised with a real multi-round "add evidence, re-derive" cycle since there's no derivation step (C) yet. |
| F — Validation loop | Reconstruction checked against source evidence, disagreement surfaced | PARTIAL | `reconstruction/validation.py`'s `validate_reconstructions()` nearest-point-matches two independent `ReconstructionResult`s within a distance threshold and reports agreements/disagreements/unmatched honestly -- no averaging, no silently picking a winner. Refuses to validate a `failed` reconstruction. **Now wired into a real workflow:** `evidence/promote_reconstruction.py`'s `promote_validated_reconstruction_to_entity()` runs validation as part of promotion, not as a dead-end function -- any disagreement downgrades the promoted Entity's and Geometry's provenance from RECONSTRUCTED to CONFLICT (not canonical, per `Provenanced.is_canonical()`), caps confidence at the agreement rate, and records the disagreement counts as a real Observation on the geometry. 9 tests (5 validation + 4 promotion-with-validation). Still not automatic on every new Session evidence (nothing currently triggers it without a caller supplying a second, independent reconstruction), and only compares two reconstructions to each other, not a reconstruction to an independent manual measurement -- that comparison still needs unit/scale reconciliation to be meaningful and remains a real follow-on. |
| G — Studio foundation | Viewport, outliner, inspector, selection | PARTIAL | `engine/render/viewport.py` (camera+frustum culling, REQ-029) and `engine/inspector/` (REQ-030) existed already. `engine/studio/` -- `Outliner`, `Selection`, `StudioSession` tying viewport+inspector+outliner+selection together, `provenance_panel()`, `visible_entities()`. **Now wired to the command pipeline (row M):** `StudioSession.create_entity()`/`move_selected()`/`delete_selected()` build the right typed Command and hand it to `WorldCommandProcessor` -- Studio never mutates `world` directly, so a Studio edit goes through the same validation/permission/event path as any other command caller. 22 tests, 522/522 passing overall. The Three.js artifact built in an earlier session is still a one-off visualization, not part of the repo. |
| H — World compilation foundation | WorldIR → render / physics / navigation representations, separately | PARTIAL | Physics representation exists and is mature (`engine/physics/*`, REQ-010-013). Render representation is viewport-only (no mesh/material compile step). Navigation representation: **not started** (no navmesh anywhere in the repo). |
| I — Large-world foundation | Spatial partitioning, local/world coordinate hierarchy, streamable cells | NOT_STARTED | `world_ir/coordinates.py` has multi-frame transforms (WGS84/UTM/ENU/local, REQ-005) but no cell/tile/streaming concept at all. |
| J — Engine extensibility | Reconstruction/AI/geometry backends replaceable via adapters | N/A yet | Nothing to make replaceable until C exists. Physics backend itself (`engine/physics/backend/interface.py`) already follows this pattern (`IPhysicsBackend`), so the precedent is established for when it's needed. |
| K — Trust (observed/inferred/uncertain/validated/generated) | Distinguished, never fabricated | **FUNCTIONAL** (pre-existing + extended) | `provenance.Provenance` enum already covers this (REQ-002/§7). Session/evidence layer defaults to `OBSERVED` and carries `Uncertainty` per item — consistent, not a new taxonomy. |
| L — Tested baseline | Evidence → Session → Merge → Reconstruction → WorldIR → Validation → Compilation, deterministic | PARTIAL | Evidence → Session → Merge is real and tested (25 tests, this pass). Reconstruction → WorldIR → Validation → Compilation don't exist yet, so the full chain isn't there — the chain is correct as far as it currently goes, not stubbed further.
| M — Command pipeline | Intent → parsing → typed command → validation → permission → WorldAPI → state change → event → provenance → new version (spec §7) | PARTIAL | `engine/commands/` -- `WorldCommandProcessor` is the sole WorldIR-mutation path: every command is validated (existence/type checks) before a `PermissionPolicy` check (only `AllowAllPolicy` exists; no auth/actor system in this repo, so allow-all is the honest real policy, not a stub) before the mutation, a real `Event` (with `actor_id`/`source_refs`) via the existing `EventBus`, and `world.version` incrementing on every successful command. 4 command kinds exist (create/move/delete an entity, add a relationship) because those are the only WorldIR mutations this repo can honestly perform right now -- no command kind exists for a capability (e.g. "simulate flood") the engine doesn't have. Created entities get `Provenance.GENERATED` (a command created them, not reality); moving an entity does not alter its existing provenance; `AddRelationshipCommand` defaults to `Provenance.INFERRED` (rejects self-relationships, requires both entities to exist). Wired into `engine/studio/StudioSession` -- Studio edits go through this exact path, never touching WorldIR directly, including geometric-adjacency inference (see row T). 25 tests total (18 pipeline + 7 Studio wiring, including the relationship command). Explicitly NOT built: NL/LLM intent parsing into these typed commands (a separate front-end this starts one stage downstream of), and real branch/copy-on-write versioning (spec's "NEW VERSION/BRANCH" is honestly represented today only as `world.version` incrementing -- full branching is Build Order steps 25+). |
| N — Scene graph (AI-free query engine) | "What's inside Room 4", "is this connected to that", answered without an LLM (spec §22/§59) | PARTIAL | `engine/scene_graph/` -- `SceneGraph` walks the `Relationship` edges every `Entity` already carries (no new data model): `contents_of()`/`container_of()` (CONTAINS/PART_OF), `supporters_of()` (SUPPORTS/RESTS_ON, generalizing `Inspector.find_supporting()`), `path_exists()` (BFS reachability, optionally kind-filtered), `query_by_kind()`/`all_edges()` for whole-graph queries. Purely read-only, same boundary as Inspector/Outliner. 12 tests, 534/534 passing overall. Not built: automatic derivation of these relationships from geometry (a wall isn't automatically marked RESTS_ON its foundation by anything in this repo -- that's real geometric reasoning, undone work) and room/wall/floor-level ontology beyond the existing flat `EntityType`. |
| O — Perception adapter interfaces | `IDepthBackend`, `ISegmentationBackend`, `ITrackBackend` (etc.) so future model integration is isolated, mirroring `IPhysicsBackend`/`IReconstructionBackend` (spec §8/§11/§19) | SCAFFOLDED | `perception/depth/interface.py` (`IDepthBackend`, `DepthMap` -- row-validated, no numpy dependency), `perception/segmentation/interface.py` (`ISegmentationBackend`, `SegmentedRegion`/`SegmentationResult` -- confidence-bounded, carries `model_name`/`model_version` for provenance per spec §48), and `perception/instances/interface.py` (`ITrackBackend`, `InstanceTrack` -- confidence-bounded, groups `SegmentedRegion`s across evidence items/frames, addressing spec §19 2D->3D INSTANCE LIFTING, flagged by the audit as a critical missing intelligence layer with zero code before this pass). Zero concrete backends implement any of the three interfaces -- no depth model, no SAM2, no real tracker (no appearance-embedding re-id, no geometric consistency check), nothing installed or adapted. 15 tests exercise the interface contracts themselves (ABC enforcement, shape/bounds validation) via minimal fakes, the same pattern `test_colmap_backend.py` uses for `IReconstructionBackend`'s honest-unavailability path. This is explicitly SCAFFOLDED, not PARTIAL: an interface with no implementation is a real, useful artifact (it lets future model integration be isolated and licensing-safe from day one) but it is not perception capability. |
| P — Ontology breadth | Room/wall/floor/door-level vocabulary, not just a flat 10-value list (spec §21) | PARTIAL | `world_ir/schema_v1.py`'s `EntityType` enum extended from 10 to 24 values, adding `ROOM`, `WALL`, `FLOOR`, `CEILING`, `ROOF`, `COLUMN`, `BEAM`, `DOOR`, `WINDOW`, `STAIRS`, `ROAD`, `CURB`, `SIDEWALK`, `INFRASTRUCTURE` alongside the original 10 (unchanged, none renamed/removed). 3 tests confirm round-trip through `Entity.to_dict()`/`from_dict()` and that no original value was disturbed. **Update 2026-09-12: the "no code assigns these types automatically" gap is now closed for the wall/floor/ceiling subset** -- `perception/geometry/planes.py` + `orientation.py` + `evidence/promote_planes.py` detect planes in a reconstruction's points and promote them as `EntityType.WALL`/`FLOOR`/`CEILING` entities (INFERRED provenance, see row T), **and for ROOM** -- `evidence/promote_rooms.py` (row T4) infers ROOM entities from closed wall-intersection-floor boundary rings with CONTAINS/PART_OF edges and area/dimension/height measurements. ROOF/DOOR/WINDOW/COLUMN/BEAM/etc. still have no assigning code anywhere. |
| Q — Technology registry | License/capability tracking for candidate perception technologies before any install (spec §10/§49) | PARTIAL | `docs/TECHNOLOGY_REGISTRY.md` -- real research entries (repo, code license, checkpoint license, commercial-use status, platform/GPU notes, confidence rating) for 5 candidates: Depth Anything/V2, MiDaS, ZoeDepth (depth) and SAM, SAM 2 (segmentation), matching the rigor/table format of `docs/RECONSTRUCTION_BACKEND_DECISION.md`. Ends with a "worth evaluating further" recommendation per category, explicitly not a final decision. Every license/hardware figure is disclosed as knowledge-based, not independently verified against a live repo/license file in this pass -- the document itself flags exactly where to double-check before any real integration commitment (checkpoint licensing on Depth Anything V2's tiers, ZoeDepth's training-data provenance). No technology from this registry has been installed or adapted. |
| R — Export system | glTF/USD/Blender/Unreal, geometry+transform+semantic identity preserved where the target format permits (spec §34) | SCAFFOLDED | `exporters/gltf/exporter.py` -- `export_to_gltf()` produces a real, spec-conformant single-file glTF 2.0 JSON (`asset.version: "2.0"`, `nodes`/`meshes`/`accessors`/`bufferViews`/`buffers` with base64-embedded binary data), one node per `Entity` that has both a `transform` position and a `BOX`-typed `Geometry` (unit cube, positioned via the node's `translation`). `exporters/usd/exporter.py` -- `export_to_usda()` produces real, hand-written USD ASCII (`.usda`) text with the same BOX+transform scope, one `Cube` prim per qualifying entity (`size = 2`, positioned via `xformOp:translate`), with entity ids sanitized into valid USD prim identifiers. 3 tests each verify actual structural/byte-level correctness (glTF accessor counts and decoded buffer length; USD prim count, translate values, and a round-trip write/read), not just "no exception". Deliberately narrow for both: entities without a transform or without `BOX` geometry are skipped, not fabricated -- `Geometry` in this schema stores only a `vertex_count` int, no real vertex/shape data, so `MESH`/`POINTCLOUD` entities have nothing genuine to export yet. No materials, textures, hierarchy beyond one flat parent, and the USD output has never been round-tripped through the real `pxr` library or any actual USD viewer -- it's hand-written text conforming to the documented grammar, not independently verified against real USD tooling. No Blender or Unreal target exists. |
| T3 — Evidence fusion (scalar quantities) | Combine independent observations of one real quantity with confidence weighting + conflict detection/resolution + provenance propagation, never destroying raw evidence (spec §14/§15) | PARTIAL (new, 2026-09-12) | `reconstruction/fusion/fusion.py` — `fuse_quantity()`: deterministic inverse-variance fusion of `FusableObservation`s (canonical input ordering, pure math, no clocks/RNG). Conflict detection at `CONFLICT_SIGMA = 5` combined standard deviations — a 5 cm LiDAR-vs-photogrammetry delta with centimeter-scale claimed uncertainties is a conflict (7.07σ) while the same delta with decimeter uncertainties is consistent, so conflict is a function of claimed uncertainty, not raw delta. Resolution is honest: fused value stays the weighted mean of ALL sources, provenance becomes `Provenance.CONFLICT` (existing vocabulary), confidence drops to the weakest contributor, every conflicting pair preserved (`MeasurementConflict` with sigma_multiple) alongside the verbatim `contributing` observations and observed spread (`agreement_rms`). Single-observation passthrough keeps provenance+precision exactly (OBSERVED stays OBSERVED). Unit discipline: mixing units raises `FusionError` (leverages `engine/core/units.py` vocabulary). `fused_to_measurement()` bridges into WorldIR `Measurement` with CONFLICT provenance surviving. 20 tests incl. the spec's canonical 3.17/3.22 scenario with hand-computed sigma multiples, input-order determinism, multi-pair preservation, and error paths. Full suite: 761 passed, 1 skipped. NOT built, labelled: robust/outlier-rejecting estimation, temporal consistency, pose-level fusion (rotations aren't scalars), and automatic callers — nothing in the repo yet holds two independent observations of the same scalar and fuses them as part of a pipeline (competing plane fits, multiple reconstruction backends are the obvious next callers). |
| T4 — Room inference (planes -> rooms) | ROOM entities with CONTAINS/PART_OF relationships and room-dimension measurements derived from wall/floor/ceiling plane structure (spec §17 "rooms", §13 "room dimensions", §18/§21 scene-graph edges) | PARTIAL (new, 2026-09-12) | `evidence/promote_rooms.py` — `detect_rooms()` + `promote_room_to_entity()`: a room is inferred only when walls REST on a floor (signed lowest-point test within 0.3 m — a wall piercing the floor plane reads as non-contact, a real bug caught: min-of-|distance| wrongly accepted piercing slabs), share a top height (within 0.25 m), and their wall∩floor LINES close a boundary ring of corners their inlier support reaches (lines are exact plane intersections, immune to the ragged AABB endpoints real grid/reconstruction edge-loss produces; co-planar wall patches merge; corners validated within 60% of each wall's span). Ring walk orients every line with the floor's inlier centroid on its left and sorts by DIRECTED angle — a convex CCW ring with opposite walls opposite (the folded-angle ordering falsely placed parallel walls adjacent: observed, fixed). Walkable-floor check: the floor needs ≥3 inliers strictly INSIDE the ring, so a horizontal slab (which classifies as a second floor — cameras above it) or the wall-base ring cannot spawn a duplicate room; the slab candidate is reported honestly as FLOOR_NOT_INTERIOR/NO_CLOSED_RING. Ceiling matched at the wall-top plane or honestly absent. Measurements (ESTIMATED, precision from fit RMS): floor area (shoelace over the ring), floor extents, height = exact floor-to-ceiling parallel-plane distance when the ceiling is observed (wall top rows are lost to the ceiling plane in real captures — measured 1.8 vs true 2.0 — so wall-tops-only height is an honestly-labelled lower bound). Promotion writes ROOM Entity (INFERRED) + CONTAINS room→part and PART_OF part→room edges (INFERRED, derivation metadata) and refuses unclosed candidates/missing plane entities. 18 tests incl. the full real pipeline (RANSAC → classification → plane promotion → room detection → promotion) on a hand-checkable 2.5×2.25×2 m scene: area 5.625 m² exact, doorway gap + co-planar doorway segment merged into one boundary line, piercing-slab rejection, open-facade UNCLOSED_RING, two-walls NO_CLOSED_RING, determinism, WorldIR round-trip, and `SceneGraph.contents_of()` answering "what does this room contain?" (6 parts) from the promoted edges alone. Suite: 798 passed, 2 skipped. NOT built, labelled: non-convex (L-shaped) rooms (conservative failure, not a silent lie — stated in the module), DOOR/WINDOW/ROOF assignment, multi-room shared-wall ownership, multi-floor rooms. |
| T5 — World compiler + validation gate | One deterministic reconstruction -> validated-WorldIR bridge; invalid entities rejected/quarantined, never silently inserted (spec §16/§24/§25) | PARTIAL (new, 2026-09-12) | `engine/compiler/world_compiler.py` — `compile_reconstruction_to_world()`: composes the pure layers (seeded RANSAC -> camera-side classification -> plane promotion -> room inference/promotion), stamps provenance (world RECONSTRUCTED without upgrading any entity), and returns `CompileDiagnostics` accounting for EVERY plane, every unassigned point (`unknown_point_fraction` = the honest known-unknown floor), and every failed room candidate with its notes — nothing silently dropped. Refusals: failed/empty reconstruction, missing cameras (`CompileInputError`). `world_ir/validation.py` — `validate_world_ir()`: composes the world's own structural self-check with geometry bounds checks (empty/inverted/non-finite), near-singular + non-affine transforms, duplicate-identity warnings, measurement sanity, and provenance/confidence consistency (UNKNOWN provenance cannot claim 0.9+ confidence); two severities, deterministic ordering; the compiler's gate raises `WorldValidationGateError` with issues attached rather than returning corrupted state. 26 tests incl. byte-identical determinism (caught a real gap: WorldIR's per-instance uuid4 `main_branch_id` default — the compiler now supplies stable identity, same discipline as created_at=0.0), seed robustness, all refusal paths, gate-failure paths, Studio consumption, round-trip. Suite: 824 passed, 2 skipped. NOT built, labelled: compiler does not yet consume depth/segmentation/material evidence (planes+rooms only), no quarantine store for rejected entities (issues surface via diagnostics/gate error). |
| U — Benchmark suite | Real timing measurements over physics/WorldIR/command-pipeline operations (spec §45/§65) | PARTIAL | `benchmarks/harness.py` (`run_benchmark()`, stdlib `time.perf_counter()` only, propagates exceptions rather than swallowing a crashing benchmark), `benchmarks/physics_bench.py` (rigid-body stepping at N=10 and N=100 bodies -- real numbers observed: ~0.024s/iter at N=10 vs ~0.71-0.90s/iter at N=100, consistent with the backend's documented naive O(n^2) broadphase, not a bug; command-pipeline throughput for 1000 `CreateEntityCommand`s, ~0.007-0.017s/iter), `benchmarks/quality_bench.py` (`compute_quality_report()` over 500 entities, ~0.0007s/iter). `benchmarks/run_all.py` actually executes all of them and prints a real table -- verified by running it, not by trusting a test mock. 6 tests (harness correctness, exception propagation, small-scale smoke tests per benchmark, `run_all()` sanity). These are wall-clock numbers from one development machine, one run each -- not portable across hardware, not an SLA, and cover exactly 3 operations. Not benchmarked (because nothing real exists yet to benchmark): reconstruction accuracy/speed at scale, export fidelity, semantic precision/recall, mesh quality -- the rest of spec item 65's list. |
| S — Quality/completeness engine | "How confident are we?" answered from real WorldIR data -- provenance mix, mean confidence, canonical fraction (spec §29/§64) | PARTIAL | `engine/quality/report.py` -- `compute_quality_report()` builds a `WorldQualityReport` from a WorldIR's actual entities: `provenance_breakdown` (real counts per `Provenance` value), `mean_confidence`/`canonical_fraction` (both `None`, not a fabricated fallback, when there are zero entities), `low_confidence_entity_ids` (delegates to `Inspector.low_confidence_entities()`, doesn't reimplement it), `unresolved_relationship_entity_ids` (UNKNOWN-kind relationships), and a human-readable `summary_text()`. 9 tests confirm exact counts/means by hand-computed assertion, not "is a float". Honest limit stated in the module itself: this is a ratio over entities that already exist in WorldIR -- it cannot detect a real-world surface that was never observed and therefore was never even instantiated as an Entity; that representational-gap problem is different and harder, and this doesn't solve it. |
| T — Geometric reasoning (adjacency) | Detect spatial relationships (ADJACENT_TO/OVERLAPS/SUPPORTS/etc) from real geometry, not guessed (spec §23) | PARTIAL | `engine/geometry/adjacency.py` -- `infer_geometric_relationships()` derives real `ADJACENT_TO`/`OVERLAPS` `Relationship` candidates from AABB (axis-aligned bounding cube: center = transform position, half-extent = radius) overlap math. `aabb_overlap()` is a standard per-axis interval test, unit-tested directly against hand-computed corner cases (touching boundary, clearly separated, one-inside-another). Pure query -- does not mutate WorldIR itself. **Now wired into the command pipeline:** `AddRelationshipCommand` (validated: both entities must exist, self-relationships rejected, defaults `provenance=INFERRED` unless the caller states otherwise) and `StudioSession.infer_and_add_adjacency()` call `infer_geometric_relationships()` and execute an `AddRelationshipCommand` per candidate, so inference now produces real, event-logged, provenance-tagged Relationships instead of being a query nobody calls -- 7 more tests cover this path. 16 tests total, including that each unordered entity pair is reported exactly once and that a missing-transform entity is skipped rather than defaulted to the origin (which would fabricate false adjacency). Deliberately does NOT infer SUPPORTS/RESTS_ON (needs real shape + gravity/contact-normal reasoning this repo has no data for) or PART_OF/CONTAINS (semantic, not geometric). Models every entity as a sphere/cube via one radius scalar -- a real oriented rectangular footprint (an actual wall) isn't representable since `Geometry` stores no real vertex/shape data yet. |
| T2 — Geometric reasoning (planes -> structure) | Real plane detection + wall/floor/ceiling classification + geometry-derived measurements from reconstructed points (spec §17/§13, first vertical-slice world understanding) | PARTIAL (new, 2026-09-12) | `perception/geometry/planes.py` -- `detect_planes()`: dependency-free deterministic RANSAC (seeded via `engine/core/rng`, 3-point consensus, least-squares Jacobi refit with re-collection rounds, canonical sorted/re-keyed output), extracts floor/ceiling/wall planes from a `ReconstructionResult`'s points with honest bookkeeping (`unassigned_point_ids` reported, never silently dropped; noise-heavy clouds yield no planes rather than garbage fits). `perception/geometry/orientation.py` -- `classify_plane()` assigns FLOOR (horizontal, normal up = camera side), CEILING (horizontal, normal away from cameras), WALL (near-vertical), UNKNOWN (everything else, honestly); requires camera positions (raises without them -- camera-side is the definition) and a caller-supplied up vector (with `up=None` everything is UNKNOWN with a "refusing to guess" note, never silently assumed). `evidence/promote_planes.py` -- `promote_plane_to_entity()`: promotes a classified plane into WorldIR as Entity(WALL/FLOOR/CEILING, provenance INFERRED) + `GeometryType.PLANE` geometry (provenance RECONSTRUCTED, bounds = real inlier AABB) + ESTIMATED extent Measurement (real inlier span, precision = plane fit RMS) + a thickness Measurement ONLY when a parallel wall counterpart face exists within `WALL_PAIR_THICKNESS_M` with sufficient span overlap (single-surface walls get none -- no fabrication; a same-facing-normal pairing bug and a sign bug in `_plane_gap` were both caught by tests and fixed). `GeometryType.PLANE` added additively to the schema (same discipline as the EntityType extension). 32 tests with hand-computed expectations (exact plane constants d in {-4, -2.5, 0, 0}, inlier counts 164/144/90/90, extent = sqrt(16+2.75^2), thickness = 0.2 m), determinism (same seed -> identical planes; seeds 7/123/9999 all find the same structure), noise-fraction limits, refusal paths, and byte-identical WorldIR round-trip. Not done: room inference (connecting planes into ROOM entities), SUPPORTS/support reasoning, evidence fusion across competing plane fits, curvature/mesh-level reasoning, real-photo validation at scale. |

## Termination test (§35), honest answers today

Can a dataset enter the system as structured Sessions? **Yes.** Can Sessions be
merged? **Yes, with honest limits.** Can real reconstruction occur? **No.** Can
results become WorldIR? **No** (nothing produces them yet). Provenance traced?
**Yes.** Uncertainty represented? **Yes.** World compiled into multiple runtime
representations? **Partially** (physics only). New evidence incrementally
improve an existing world? **The data model supports it; nothing yet re-derives
on new evidence, since nothing derives at all.**

**Loop verdict: keep going — do not stop.** Reconstruction (Goal C) is the
next foundational blocker; everything from D onward is downstream of it.

## Next highest-leverage blocker

Reconstruction backend evaluated and decided: **COLMAP** (BSD license,
subprocess-boundary integration, sparse output maps directly onto existing
`Provenance.RECONSTRUCTED` + `Observation`/`Measurement` records). Full
rationale, alternatives considered (ODM, Meshroom/AliceVision, OpenSfM), and
why each was rejected: `docs/RECONSTRUCTION_BACKEND_DECISION.md`.

Real COLMAP integration code now exists (`ColmapReconstructionBackend`):
real subprocess pipeline, real parsing of COLMAP's documented text format,
verified against literal COLMAP-format fixtures (481/481 tests, 6 new).

**Update (2026-09-12):** COLMAP 4.2.0 (no-CUDA Windows build) was installed
and `ColmapReconstructionBackend.reconstruct()` was run for real against
4 photos of a public test scene (`kicker` from the
`alexmkwizu/colmap-testing-dataset` HF dataset). Result: 3/4 images
registered, 228 real 3D points, ~23s. This was not a no-op -- the run
surfaced two real bugs in `colmap_backend.py`, now fixed: (1) COLMAP's
Windows CLI needs `QT_QPA_PLATFORM=offscreen` or it blocks on a GUI
dialog; (2) this build has no GPU, so the feature extractor/matcher need
`use_gpu=0` explicitly or they hard-fail. Both fixes are unconditional
(`_subprocess_env()`, `use_gpu` constructor flag), not environment-specific
hacks.

Goal F (validation) no longer requires C to be fully done -- it only needs
two `ReconstructionResult`s to compare, and the fake backend already
produces those. `validate_reconstructions()` now exists (486/486 tests,
5 new) and surfaces point-level disagreement without fabricating agreement.

**Update (2026-09-12):** Validation wired into promotion.
`promote_validated_reconstruction_to_entity()` in
`evidence/promote_reconstruction.py` now calls `validate_reconstructions()`
as part of promoting a reconstruction to WorldIR, and its result changes
real state rather than being discarded: disagreement flips the promoted
Entity/Geometry's provenance to CONFLICT and records the disagreement
counts as an Observation. 9 tests, 489/489 passing overall.

Remaining open item, not a blocker but explicitly deferred:
comparing a reconstruction against an independent manual measurement
(rather than reconstruction-vs-reconstruction) needs a real scale/unit
reconciliation step first -- COLMAP's monocular sparse output has no
absolute scale without ground-control points, so a raw distance
comparison against a manually measured real-world distance would be
comparing incompatible units. That reconciliation is real, undone work,
not a small wiring step.

Remaining honest gaps on reconstruction itself (not blockers, but real
limits): only one small 4-image scene has been exercised -- no large
building, no stress test, no automated CI coverage of the real binary
(only the pure parsing functions are unit-tested against fixture text).
