import CaptureApp from '@/apps/capture/CaptureApp';

export const metadata = {
  title: 'Reality Capture Instrument | Reality Engine',
  description: 'Precision reality capture mobile instrument with multi-pass guidance.',
};

export default function CapturePage() {
  return <CaptureApp />;
}
