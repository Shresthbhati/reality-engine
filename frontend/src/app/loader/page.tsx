import LoaderApp from '@/apps/loader';

export const metadata = {
  title: 'Evidence Loader Workstation | Reality Engine',
  description: 'Evidence ingestion, synchronization and diagnostics suite for computer and phone.',
};

export default function LoaderPage() {
  return <LoaderApp />;
}
